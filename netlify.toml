[build]
  command = "echo 'Pre-built output - no rebuild needed'"
  publish = ".next/server/app"

[build.environment]
  NODE_VERSION = "20"

# Catch-all redirect to index (as fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
